<div class="card  show-card" style="w-100;height: 40rem">
  <div class="row no-gutters">
    <div class="col-md-4">
      <img src="{{place.imgPath}}" class="card-img" alt="{{place.imgName}}">
      <h3 class="card-title ml-4 mt-3">{{place.name}}</h3>
    </div>
    <div class="col-md-8">
      <div class="card-body">
        <p class="card-text"><strong>Descripción:</strong><br>{{place.description}}</p>
        <p class="card-text">Precio: $ {{place.price}}</p>
      </div>
      <ul class="list-group list-group-flush">
      <li class="list-group-item"><strong>Dirección</strong> <br>{{place.address}}</li>
      <li class="list-group-item"> <strong>Disponible</strong> <br>Fecha de inicio: {{place.startDate}} <br> Fecha límite: {{place.endDate}}</li>
      <li class="list-group-item"> <strong>Información del anfitrión</strong> <br>Usuario: {{user.username}} <br> Email: {{user.email}} <br> Teléfono: {{user.telephoneNumber}}</li>
    </ul>
    </div>
    
  </div>
</div>